#include <iostream>
#include <stack>
using namespace std;

int main(){

void push(stack *s, int value){
    if(s->count==MAX){
        cout<<"stack full";
    } else {
        for(int i=0;i<n;i++){
            if(value%2!=0){
                s->data[q->count]=value;
                s->count++
            }
        }
    }
}

for(int i=0;i<n;i++){
    if(n%2!=0)
    return n;
}

int n;
struct stack{
        int data[n];
        int count;
    };
    
    stack antrian; 

    cout<<"masukan n: ";
    cin>>n;
    cout<<"Bilangan ganjil yang ada di stack"<<;
}